server {
    listen 80;
    server_name www.db.com db.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_name www.db.com db.com;

    ssl_certificate     /etc/nginx/certs/www.db.com.crt;
    ssl_certificate_key /etc/nginx/certs/www.db.com.key;

    location / {
        proxy_pass http://llibres_phpmyadmin:80;
    }
}
